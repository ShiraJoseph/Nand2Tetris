//Design a NOT gate
//in Hardware Description Language
//using only given, primitive Nand chips

/**
 * out = not in
 */

CHIP Not {
    IN in;
    OUT out;

    PARTS:
      Nand(a=in,b=in,out=out);
}
